<% if @answer.errors.present? %>
    <% @answer.errors.full_messages.each do |message| %>
        $('.answer[data="' + <%= @answer.id %>+'"]').prepend('<%= j message %>');
    <% end %>
<% else %>
    $('.answer[data="' + <%= @answer.id %> + '"]').replaceWith('<%= j render @answer %>');
<% end %>