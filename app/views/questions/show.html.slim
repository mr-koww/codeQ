= render @question, show_attachments: true
|------------------------
.answers
  = render 'answers/answers'
|------------------------
- if user_signed_in?
  = render 'answers/new', question: @question, answer: @answer